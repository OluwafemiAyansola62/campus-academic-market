import {NextResponse} from 'next/server';
export async function POST(req:Request){try{const {plan,amount,email}=await req.json();if(!email||!email.includes('@'))return NextResponse.json({error:'A valid email is required.'},{status:400});const numeric=Number(amount);if(!numeric||numeric<100)return NextResponse.json({error:'Invalid payment amount.'},{status:400});const secret=process.env.FLW_SECRET_KEY;if(!secret)return NextResponse.json({error:'Flutterwave is not configured yet. Add FLW_SECRET_KEY in Vercel Environment Variables.'},{status:503});const base=process.env.NEXT_PUBLIC_SITE_URL||new URL(req.url).origin;const txRef=`CAM-${Date.now()}-${Math.random().toString(36).slice(2,9)}`;const response=await fetch('https://api.flutterwave.com/v3/payments',{method:'POST',headers:{Authorization:`Bearer ${secret}`,'Content-Type':'application/json'},body:JSON.stringify({tx_ref:txRef,amount:numeric,currency:'NGN',redirect_url:`${base}/success`,customer:{email},customizations:{title:'Campus Academic Market',description:`${plan} membership`,logo:`${base}/icon.svg`},meta:{plan}})});const data=await response.json();if(!response.ok||data.status!=='success')return NextResponse.json({error:data.message||'Flutterwave could not initialize payment.'},{status:502});return NextResponse.json({link:data.data.link,tx_ref:txRef});}catch{return NextResponse.json({error:'Unable to initialize payment.'},{status:500})}}
