'use client';
import {useState} from 'react';
import {Check,LoaderCircle,Sparkles} from 'lucide-react';
import {plans} from '@/lib/data';
export function Pricing(){const [loading,setLoading]=useState('');async function pay(plan:{name:string;price:number}){if(!plan.price)return document.getElementById('resources')?.scrollIntoView({behavior:'smooth'});setLoading(plan.name);try{const r=await fetch('/api/payments/initialize',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({plan:plan.name,amount:plan.price,email:prompt('Enter your email for payment')||''})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Payment could not start');window.location.href=data.link}catch(e){alert(e instanceof Error?e.message:'Payment error');setLoading('')}}return <div className="pricing-grid">{plans.map((p)=><div key={p.name} className={'price-card card reveal '+(p.featured?'featured':'')}>{p.featured&&<div className="popular"><Sparkles size={13}/> MOST POPULAR</div>}<h3>{p.name}</h3><p>{p.desc}</p><div className="price">₦{p.price.toLocaleString()}<small>{p.period}</small></div><ul>{p.features.map(f=><li key={f}><Check size={16}/>{f}</li>)}</ul><button className={'btn '+(p.featured?'btn-primary':'btn-secondary')} onClick={()=>pay(p)} disabled={!!loading}>{loading===p.name?<LoaderCircle className="spin" size={17}/>:p.cta}</button></div>)}<style jsx>{`.pricing-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}.price-card{padding:30px;position:relative}.price-card h3{font:800 25px Manrope;margin:0 0 10px}.price-card p{color:var(--muted);line-height:1.65;min-height:52px}.price{font:800 36px Manrope;letter-spacing:-.06em;margin:25px 0}.price small{font:500 12px DM Sans;color:var(--muted);letter-spacing:0}.price-card ul{list-style:none;padding:0;margin:0 0 25px;display:grid;gap:13px}.price-card li{display:flex;gap:10px;align-items:center;font-size:14px}.price-card li svg{color:#0f766e}.price-card .btn{width:100%}.featured{border:1.5px solid #83cdb2;box-shadow:0 25px 70px rgba(15,118,110,.12);transform:translateY(-8px)}.popular{position:absolute;right:20px;top:18px;background:#dff6ed;color:#0f5f58;border-radius:999px;padding:6px 9px;font-size:9px;font-weight:800;display:flex;align-items:center;gap:5px}.spin{animation:spin 1s linear infinite}@keyframes spin{to{transform:rotate(360deg)}}@media(max-width:900px){.pricing-grid{grid-template-columns:1fr}.featured{transform:none}}`}</style></div>}
